# config/sidekiq.yml
# Pure YAML (ERB is ok). No Ruby here.
concurrency: <%= Integer(ENV.fetch("SIDEKIQ_CONCURRENCY", 5)) %>
timeout: 30

# Weighted queues (higher number = higher priority)
queues:
  - [default, 5]
  - [notifications, 3]
  - [view_counts, 2]
  - [timeline_cache, 1]
